# Prompt Log — Лабораторная работа №14

## Задание 1 — Разработка сборщика данных на Go

### Промпт 1
**Инструмент:** DeepSeek
**Промпт:** "Реализуй на Go параллельный парсер интернет-магазина books.toscrape.com. Требования: горутины для параллельного сбора 5+ страниц, канал с буфером 200 для передачи продуктов, WaitGroup для синхронизации, извлечение полей title, price, rating, page_url, collected_at. Сохраняй результат в JSON Lines (каждая запись на отдельной строке). Используй golang.org/x/net/html для парсинга. При ошибке на одной странице остальные продолжают работу."
**Результат:** Получила полностью рабочий сборщик с параллельным парсингом 50 страниц, корректным извлечением данных и сохранением в JSON.

---

## Задание 2 — Буферизация и пакетная запись

### Промпт 1
**Инструмент:** DeepSeek
**Промпт:** "Добавь в Go-сборщик пакетную запись с буферизацией. Реализуй структуру BatchWriter с полями buffer (срез Product), bufferSize int, flushInterval time.Duration. Методы: Add(product) для добавления в буфер с автоматическим сбросом при заполнении, autoFlush() для сброса по таймеру в фоновой горутине, Close() для финального сброса. Используй мьютекс для потокобезопасности. Параметры: bufferSize=10, flushInterval=3 секунды."
**Результат:** Получила BatchWriter с корректной синхронизацией, автоматическим сбросом по таймеру и финальным сбросом при закрытии.

---

## Задание 3 — Graceful Shutdown

### Промпт 1
**Инструмент:** DeepSeek
**Промпт:** "Добавь в Go-сборщик обработку сигналов SIGINT и SIGTERM. Используй канал os.Signal и signal.Notify. При получении сигнала: перестань принимать новые задачи, дождись завершения текущих горутин парсинга через WaitGroup, закрой канал с продуктами, вызови Close() у BatchWriter для сброса буфера, выведи итоговую статистику. Данные не должны теряться при прерывании."
**Результат:** Graceful shutdown работает корректно — при Ctrl+C программа выводит сообщение и сбрасывает буфер в файл.

---

## Задание 4 — Импорт данных в Python (Polars)

### Промпт 1
**Инструмент:** DeepSeek
**Промпт:** "Напиши Python-скрипт с Polars для загрузки JSON Lines файла. Построчно прочитай файл, собери список словарей, создай DataFrame. Выведи: первые 5 строк, количество строк и столбцов, типы колонок через df.schema, количество пропусков через df.null_count(). Используй from __future__ import annotations если нужно. Кодировка UTF-8."
**Результат:** Данные загружены корректно. Цена и рейтинг пришли как строки, пропусков нет.

---

## Задание 5 — Очистка и валидация данных

### Промпт 1
**Инструмент:** DeepSeek
**Промпт:** "Выполни очистку данных в Polars. Шаги: удали дубликаты через df.unique(), преобразуй цену из строки '£51.77' в Float64 (удали символ валюты и запятые, приведи к типу), преобразуй рейтинг из слов 'One'/'Two'/'Three'/'Four'/'Five' в числа 1-5 через replace_strict с маппингом. После каждого шага выводи промежуточный результат. Пропуски после очистки проверь через null_count()."
**Результат:** Цена стала Float64 (средняя 33.71), рейтинг стал Int64 (распределение: 1→8, 2→8, 3→5, 4→6, 5→13).

---

## Задание 6 — Агрегационный анализ

### Промпт 1
**Инструмент:** DeepSeek
**Промпт:** "Выполни агрегацию в Polars: сгруппируй данные по полю rating, вычисли для каждой группы среднюю цену (mean), минимальную (min), максимальную (max) и количество записей (len). Отсортируй по рейтингу. Выведи результат в читаемом табличном виде. Добавь также общую статистику по всем товарам (глобальные min, max, mean, std, count)."
**Результат:** Получена таблица: рейтинг 1 (средняя цена 30.40, 8 книг), рейтинг 2 (34.27, 8), рейтинг 3 (35.28, 5), рейтинг 4 (37.06, 6), рейтинг 5 (33.27, 13).

---

## Задание 7 — Сохранение в Parquet

### Промпт 1
**Инструмент:** DeepSeek
**Промпт:** "Сохрани очищенный Polars DataFrame в формат Parquet. Используй метод write_parquet() с путём '../data/products.parquet'. После сохранения прочитай файл обратно через read_parquet() и выведи первые 3 строки для верификации. Замерь размер файла через os.path.getsize() и сравни с исходным JSON."
**Результат:** Parquet-файл создан. Типы данных сохранены корректно (числа остались числами).

---

## Задание 8 — Анализ через DuckDB (SQL)

### Промпт 1
**Инструмент:** DeepSeek
**Промпт:** "Выполни анализ Parquet-файла через DuckDB. Напиши SQL-запрос: SELECT rating, COUNT(*) as count, AVG(price) as avg_price, MIN(price), MAX(price) FROM 'products.parquet' GROUP BY rating ORDER BY rating. Замерь время выполнения через time.time() до и после запроса. Сравни с производительностью Polars из задания 6 (визуально или замерь там тоже)."
**Результат:** DuckDB выполнил запрос за 6 секунд (на 40 строках накладные расходы выше, чем польза).

---

## Задание 9 — Визуализация результатов

### Промпт 1
**Инструмент:** DeepSeek
**Промпт:** "Построй два графика с Matplotlib: горизонтальная гистограмма распределения книг по рейтингу (rating → count) и вертикальная гистограмма средней цены по рейтингу (rating → avg_price). Оформи: подписи осей, заголовки, сетку, разные цвета (skyblue для первого, salmon для второго). Сохрани в PNG с разрешением 150 dpi через plt.savefig(). Используй plt.tight_layout() чтобы не обрезалось."
**Результат:** Графики сохранены в data/charts.png. На первом графике видно, что больше всего книг с рейтингом 5 (13 штук), на втором — самая высокая средняя цена у рейтинга 4 (£37.06).

---

## Задание 10 — Документирование конвейера

### Промпт 1
**Инструмент:** DeepSeek
**Промпт:** "Создай README.md для конвейера обработки данных. Включи: схему архитектуры (Go-сборщик → JSON → Polars → Parquet → DuckDB → визуализация), инструкцию по запуску (отдельно для Go и Python), примеры запросов (Polars и DuckDB), таблицу с результатами агрегации, выводы о производительности и качестве данных. Формат — обычный текст без маркдауна (потому что копирую без форматирования)."
**Результат:** Получен полный README с архитектурой, командами запуска и выводами.

---

## Итого

- **Количество промптов:** 10 (по одному на задание)
- **Что пришлось исправлять вручную:** формат даты в strptime (time_format → format), установка зависимостей через pip, путь к JSON-файлу
- **Время выполнения:** ~2 часа